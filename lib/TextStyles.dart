import 'package:flutter/material.dart';

class AppTextStyles {
  static const TextStyle titleStyle = TextStyle(
      fontSize: 24, fontWeight: FontWeight.bold, fontFamily: 'san fran'
      // Add other properties like color, letterSpacing, etc., as needed
      );

  static const TextStyle subtitleStyle = TextStyle(
    fontSize: 16,
    // Add other properties as needed
  );

  // Add more text styles as per your requirement
}
